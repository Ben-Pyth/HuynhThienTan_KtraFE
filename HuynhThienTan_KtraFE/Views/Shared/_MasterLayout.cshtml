<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Điện Thoại - Máy Tính Bảng</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }
        #top {
            width: 100%;
            background-color: #ffcc00;
            display: flex;
            flex-direction: column; /* Sắp xếp theo chiều dọc */
            justify-content: center; /* Căn giữa theo chiều ngang */
            align-items: center;
            padding: 0 20px;
            box-sizing: border-box;
        }

            #top h1 {
                color: white;
                margin: 10px 0;
            }

        #header-bar {
            display: flex; /* Sắp xếp tiêu đề và thanh tìm kiếm theo chiều ngang */
            justify-content: space-between;
            align-items: center;
            width: 100%;
        }
            #header-bar h1 {
                color: white;
                margin: 10px 0;
            }


        #searchBar {
            display: flex;
            align-items: center;
            flex-grow: 1;
            justify-content: flex-end;
        }

            #searchBar input[type="text"] {
                width: 100%;
                padding: 8px;
                font-size: 16px;
                border-radius: 5px;
                border: 1px solid #ccc;
            }

            #searchBar button {
                padding: 8px 15px;
                margin-left: 10px;
                background-color: #4CAF50;
                color: white;
                border: none;
                border-radius: 5px;
                cursor: pointer;
            }

            #searchBar img {
                margin-left: 10px;
                width: 80px; /* Phóng to logo */
                height: 80px; /* Phóng to logo */
            }
        #nav-buttons {
            display: flex;
            align-items: center;
            gap: 15px; /* Khoảng cách giữa 2 nút */
        }

        #home-button, #account-button {
            padding: 10px 20px;
            background-color: white;
            border: 1px solid #ccc;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }

            #home-button:hover, #account-button:hover {
                background-color: #ff9900;
                color: white;
            }

        #banner {
            margin-top: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #ffcc00;
            padding: 10px;
            border-radius: 5px;
            width: 100%;
        }

            #banner button {
                background-color: white;
                border: 1px solid #ccc;
                color: black;
                padding: 8px 12px;
                border-radius: 5px;
                font-size: 14px;
                cursor: pointer;
                transition: background-color 0.3s ease;
            }

                #banner button:hover {
                    background-color: #ff9900;
                    color: white;
                }
        #body {
            display: flex; /* Thêm flex để tạo bố cục ngang */
            justify-content: space-between; /* Căn giữa và giãn cách giữa các phần tử */
            padding: 20px;
            box-sizing: border-box;
        }

        /* Tiêu đề phần sản phẩm nổi bật */
        #section-title {
            text-align: center;
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        #slider {
            position: relative;
            width: 100%;
            max-width: 900px; /* Giới hạn chiều rộng slider */
            margin: 0 auto; /* Canh giữa slider */
            overflow: hidden; /* Ẩn phần vượt ngoài */
            margin-bottom: 20px; /* Khoảng cách dưới slider */
        }

        .slider-container {
            display: flex; /* Sắp xếp các slide theo hàng ngang */
            transition: transform 0.5s ease; /* Hiệu ứng chuyển động */
            width: 300%; /* Đảm bảo đủ không gian cho 3 slide */
        }

        .slide {
            flex: 1; /* Mỗi slide chiếm 1/3 chiều rộng */
            box-sizing: border-box;
        }

            .slide img {
                width: 100%; /* Đặt chiều rộng hình ảnh là 100% của slide */
                height: auto;
                border-radius: 10px;
            }

        /* Nút điều hướng */
        .slide-button {
            position: absolute;
            top: 50%; /* Đặt nút ở giữa chiều cao */
            transform: translateY(-50%); /* Đưa nút lên giữa theo chiều dọc */
            background-color: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            z-index: 1;
        }

        .prev {
            left: 10px;
        }

        .next {
            right: 10px;
        }

        .slide-button:hover {
            background-color: rgba(0, 0, 0, 0.8);
        }

        /* Phần sản phẩm khác */
        #mainContent {
            width: 100%;
            margin-top: 20px;
        }

        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); /* Căn chỉnh sản phẩm thành lưới */
            gap: 20px;
        }

        .product-card {
            background-color: white;
            border-radius: 10px;
            padding: 10px;
            text-align: center;
            transition: transform 0.2s ease;
        }

            .product-card:hover {
                transform: translateY(-5px);
            }

        .product-image img {
            width: 100%;
            height: auto;
            border-radius: 10px;
        }

        .product-title {
            margin: 10px 0;
            font-size: 16px;
            font-weight: bold;
        }

        .product-price {
            color: #4CAF50;
            font-size: 18px;
            display: inline-block;
            text-align: center;
        }


        #right_sidebar {
            width: 200px; /* Đặt chiều rộng cố định cho sidebar */
            background-color: white;
            padding: 10px;
            box-sizing: border-box;
        }

            #right_sidebar button {
                width: 100%;
                margin-bottom: 10px;
                background-color: lightblue;
                color: black;
                border: none;
                padding: 10px;
                cursor: pointer;
                text-align: left;
            }
        #bottom {
            width: 100%;
            background-color: black;
            color: white;
            text-align: center;
            padding: 10px 0;
        }
    </style>
</head>
<body>
    @RenderBody()
</body>
</html>


